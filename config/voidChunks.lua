
return {
    voidChunks = {
        -- The chance of a void chunk generating when it's at its minimum distance from spawn.
        minChance = 0.06,

        -- The chance of a void chunk generating when it's at its maximum distance from spawn or farther.
        maxChance = 0.55,

        -- Minimum distance from spawn at which a void chunk can generate.
        -- This is where the chance of a void chunk is at its minimum.
        minDistance = 96,

        -- The distance at which the void chunk chance is at its maximum.
        maxDistance = 1200,

        -- When the "Reduced Void Chunk Chance" upgrade is unlocked, this is the new void chunk chance.
        upgradedMaxChance = 0.48,
    },
}
